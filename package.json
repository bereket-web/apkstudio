{
  "name": "morsemaster-android",
  "version": "1.0.0",
  "private": true,
  "description": "MorseMaster packaged as an Android app using Capacitor",
  "scripts": {
    "sync": "npx cap sync android",
    "android:add": "npx cap add android"
  },
  "dependencies": {
    "@capacitor/core": "^7.0.0"
  },
  "devDependencies": {
    "@capacitor/android": "^7.0.0",
    "@capacitor/cli": "^7.0.0"
  }
}
